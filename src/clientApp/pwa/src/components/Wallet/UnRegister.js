import {Link} from "react-router-dom";

const UnRegister = () => {

    return (
        <div>

            <Link to={"/contract"} className="btn btn-primary"> درخواست مفید کارت </Link>
        </div>
    )
}

export  default  UnRegister;